install EXPRESS:
    yarn add express

install eslint:
    yarn add eslint --save-dev

install babel-eslint:
    yarn add babel-eslint --save-dev

install nodemon: dung de tu reload khi chung tu update code
    yarn add nodemon --save-dev

install module babel pakages:
    yarn add @babel/core @babel/cli @babel/node @babel/preset-env @babel/plugin-transform-runtime --save-dev
    yarn add @babel/runtime --save

install babel-plugin-module-resolver:
    yarn add babel-plugin-module-resolver --save-dev

install mongoDB:
    yarn add mongodb --save

install dotenv: (De su dung file .env)
    yarn add dotenv --save

install joi: (validate data before insert to database)
    document: https://joi.dev/api/?v=17.6.0
    yarn add joi --save

install body-parser:
    yarn ad body-parser --save

install Lodash:
    yarn add lodash --save